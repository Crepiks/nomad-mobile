import "package:flutter/material.dart";

class TheorySpaceRenderer extends StatelessWidget {
  const TheorySpaceRenderer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 50);
  }
}
