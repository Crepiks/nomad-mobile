name: nomad
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dartz:
  dotted_border: ^2.0.0+1

  mask_text_input_formatter: ^2.0.0
  cupertino_icons: ^1.0.2
  font_awesome_flutter: ^9.1.0
  fl_chart: ^0.40.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/practice-images/
